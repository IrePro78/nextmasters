query ProductsGetByCollectionSlug(
	$slug: String!
	$take: Int
	$skip: Int
) {
	collectionBySlug(slug: $slug) {
		products(take: $take, skip: $skip) {
			id
			name
			description
			product_image
			slug
			price
			categories {
				name
			}
		}
	}
}
