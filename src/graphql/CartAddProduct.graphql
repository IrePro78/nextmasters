mutation CartAddProduct(
	$productId: ID!
	$orderId: ID!
	$quantity: Int!
) {
	createOrderItem(
		createOrderItemData: {
			productId: $productId
			orderId: $orderId
			quantity: $quantity
		}
	) {
		id
		quantity
	}
}
