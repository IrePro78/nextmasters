mutation CartAddProduct(
	$productId: ID!
	$orderId: ID!
	$quantity: Int!
) {
	createOrderItem(
		createOrderItemData: {
			orderId: $orderId
			productId: $productId
			quantity: $quantity
		}
	) {
		id
		quantity
		product {
			id
			name
			price
		}
	}
}
