mutation CartAddProduct(
	$productId: ID!
	$orderId: ID!
	$quantity: Int!
	$total: Float!
) {
	createOrderItem(
		createOrderItemData: {
			orderId: $orderId
			productId: $productId
			quantity: $quantity
			total: 99.99
		}
	) {
		id
		quantity
		product {
			id
			name
			price
		}
	}
}
